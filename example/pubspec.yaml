name: example
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  auth_management:
    path: ../

  flutter_andomie: ^0.7.0
  flutter_androssy: ^0.3.5
  firebase_core: ^4.3.0
  google_sign_in: ^7.2.0
  firebase_auth: ^6.1.3
  shared_preferences: ^2.3.2
  flutter_entity: ^1.2.7

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:

  uses-material-design: true